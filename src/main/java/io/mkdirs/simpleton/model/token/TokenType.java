package io.mkdirs.simpleton.model.token;

public enum TokenType {

    INTEGER_LITERAL,
    FLOAT_LITERAL,
    STRING_LITERAL,
    CHARACTER_LITERAL,
    BOOLEAN_LITERAL,

    PLUS,
    MINUS,
    TIMES,
    DIVIDE,

    END_OF_LINE
}
